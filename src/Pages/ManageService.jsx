import { useEffect } from "react";

const ManageService = () => {

    useEffect(() => {
        document.title = 'Manage Service || Service Orbit'
    }, []);

    return (
        <div>

        </div>
    );
};

export default ManageService;